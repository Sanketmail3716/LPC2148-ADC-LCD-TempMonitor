# LPC2148-ADC-LCD-TempMonitor
This project reads analog input and temperature using LPC2148's ADC channels and displays the values on a 16x2 LCD. It uses an LM35 sensor and converts ADC data to temperature in °C. Ideal for embedded system beginners and LPC2148 ADC interfacing practice.
